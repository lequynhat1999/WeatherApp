export interface WeatherData {
  // Nhiệt độ
  Temperature: number;
  // Nhiệt độ tối thiểu
  TemperatureMin: number;
  // Nhiệt độ tối đa
  TemperatureMax: number;
  // Độ ẩm
  Humidity: number;
  // Sức gió
  Wind: number;
  // Tên thành phố
  CityName: string;
}
